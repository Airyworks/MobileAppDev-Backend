const login = require('./login')
const friends = require('./friends')
const logout = require('./logout')
const channels = require('./channels')

module.exports = {
  login, friends, logout, channels
}